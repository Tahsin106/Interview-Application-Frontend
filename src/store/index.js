import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
    // addJoinInfo: (state,[_joinCode, _role]) => {
      
    //   state.joinCode = _joinCode;
    //   state.role = _role;
    //   alert(state.joinCode)
    // }
  },
  actions: {
    // addJoinAction(context, {d1, d2}){
    //   this.commit('addJoinInfo',[d1, d2]);
    // }
  },
  modules: {
  }
})
